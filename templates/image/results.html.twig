{% extends "base.html.twig" %}

{% block title %}Экран 2{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/styles.css') }}">
{% endblock %}

{% block body %}
    <h1>Извлечены изображения из {{ url }}</h1>

    <div class="image-grid">
        {% for row in imageInfo|batch(4) %}
            <div class="row">
                {% for image in row %}
                    <div class="image-cell">
                        <img src="{{ image.url }}" alt="Image">
                    </div>
                {% endfor %}
            </div>
        {% endfor %}
    </div>

    <p>На странице обнаружено {{ imageInfo|length }} изображений на {{ totalSizeMb }} Мб.</p>

{% endblock %}